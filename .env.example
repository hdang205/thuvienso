# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=thuvienso_fbu

# Environment
NODE_ENV=development

# API Base URL (for frontend to connect to backend)
API_BASE_URL=http://localhost:8000/api

# Admin seeding via ensure_admin_user management command
# Run: python manage.py ensure_admin_user
# Required when seeding an admin account during deploy
ADMIN_EMAIL=admin@library.edu
ADMIN_PASSWORD=change_me_to_a_strong_password
# Optional: defaults to ADMIN_EMAIL if not set
# ADMIN_USERNAME=admin
# ADMIN_FIRST_NAME=Admin
# ADMIN_LAST_NAME=Library
# ADMIN_ROLE=librarian